; WORLD DOMINION uses Enhanced Input exclusively. UInputAction / UInputMappingContext
; objects are constructed and bound directly in C++ (see AWDPlayerCharacter) so the
; vertical slice is playable without first authoring .uasset Input Action assets in the
; editor. This file intentionally carries no legacy ActionMappings/AxisMappings.
[/Script/Engine.InputSettings]
bEnableMouseSmoothing=True
bEnableFOVScaling=True
ConsoleKeys=Tilde
